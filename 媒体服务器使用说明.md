# RTSP/RTMP 媒体服务器使用说明

## 项目概述

这是一个**完整的 RTSP/RTMP 服务器**，接收推流并转换为 WebRTC 和 HLS。

**架构**：
```
推流端 (FFmpeg/OBS)  →  你的媒体服务器  →  HLS 文件 + WebRTC 流
                       (RTSP/RTMP 服务器)
```

---

## 快速开始

### 1. 编译

```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls
mkdir -p build && cd build
cmake .. && make -j4
```

生成两个可执行文件：
- `stream_converter` - 拉流模式（旧版本）
- `media_server` - **服务器模式（新版本）** ← 使用这个

---

### 2. 启动媒体服务器

```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls/build

# 使用默认配置启动
./media_server

# 自定义端口
./media_server --rtsp-port 8555 --hls-dir /tmp/hls_output
```

**输出示例**：
```
========================================
RTSP/RTMP 转 WebRTC/HLS 媒体服务器
========================================

启动媒体服务器...
  RTSP 端口：8554
  RTMP 端口：1935
  HLS 目录：./hls_output

推流地址示例:
  FFmpeg 推流：ffmpeg -re -i input.mp4 -c copy -f rtsp rtsp://localhost:8554/live/mystream
  FFmpeg 推流：ffmpeg -re -i input.mp4 -c copy -f flv rtmp://localhost:1935/live/mystream
```

---

### 3. 推流测试

#### 方式 A：使用 FFmpeg 推流（RTSP）

```bash
# 推送本地视频文件
ffmpeg -re -i /path/to/video.mp4 \
       -c copy -f rtsp \
       rtsp://localhost:8554/live/mystream

# 推送摄像头（Linux）
ffmpeg -f v4l2 -i /dev/video0 \
       -f alsa -i default \
       -c:v libx264 -c:a aac \
       -f rtsp rtsp://localhost:8554/live/camera
```

#### 方式 B：使用 FFmpeg 推流（RTMP）

```bash
ffmpeg -re -i /path/to/video.mp4 \
       -c copy -f flv \
       rtmp://localhost:1935/live/mystream
```

#### 方式 C：使用 OBS 推流

**OBS 设置**：
1. 设置 → 推流
2. 服务：自定义
3. 服务器：`rtmp://localhost:1935/live`
4. 推流码：`mystream`
5. 开始推流

#### 方式 D：从 Windows 摄像头推流

在 Windows 上：
```cmd
ffmpeg -f dshow -rtbufsize 1500M -i "video=Integrated Camera" ^
       -c:v libx264 -preset ultrafast -tune zerolatency ^
       -g 60 -f rtsp rtsp://你的 Linux IP:8554/live/camera
```

---

### 4. 查看 HLS 输出

服务器会在指定目录生成 HLS 文件：

```bash
ls -lh ./hls_output/
# 应该看到：
# - playlist.m3u8
# - segment_000.ts
# - segment_001.ts
# - ...
```

**播放 HLS**：

```bash
# VLC 本地播放
vlc ./hls_output/live/mystream/playlist.m3u8

# HTTP 服务器
cd ./hls_output
python3 -m http.server 8080
# 浏览器访问：http://localhost:8080/live/mystream/playlist.m3u8
```

---

## 命令行选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| `--rtsp-port <port>` | RTSP 服务器端口 | 8554 |
| `--rtmp-port <port>` | RTMP 服务器端口 | 1935 |
| `--hls-dir <dir>` | HLS 输出目录 | ./hls_output |
| `--no-rtsp` | 禁用 RTSP 服务器 | - |
| `--no-rtmp` | 禁用 RTMP 服务器 | - |
| `--no-hls` | 禁用 HLS 输出 | - |
| `-h, --help` | 显示帮助 | - |

---

## 完整测试流程

### 测试 1：本地文件推流

**终端 1** - 启动服务器：
```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls/build
./media_server
```

**终端 2** - FFmpeg 推流：
```bash
ffmpeg -re -i ../test_source.mp4 \
       -c copy -f rtsp \
       rtsp://localhost:8554/live/test
```

**终端 3** - 播放 HLS：
```bash
vlc ./hls_output/live/test/playlist.m3u8
```

---

### 测试 2：网络摄像头推流（跨机器）

**Windows 电脑** - 推流摄像头：
```cmd
ffmpeg -f dshow -rtbufsize 1500M -i "video=Integrated Camera" ^
       -c:v libx264 -preset ultrafast ^
       -f rtsp rtsp://192.168.1.5:8554/live/webcam
```

**Linux 服务器** - 接收并转 HLS：
```bash
./media_server --rtsp-port 8554
```

**任何设备** - 播放：
```
http://192.168.1.5:8080/live/webcam/playlist.m3u8
```

---

## 当前状态

| 功能 | 状态 | 说明 |
|------|------|------|
| **RTSP 服务器** | ✅ 基础实现 | 支持 DESCRIBE, SETUP, PLAY, TEARDOWN |
| **RTMP 服务器** | 🚧 待实现 | 框架已搭建 |
| **HLS 输出** | 🚧 待完善 | 需要整合 FFmpeg HLS muxer |
| **WebRTC 推送** | ✅ 接口就绪 | 空实现，等待集成 libwebrtc |

---

## 下一步：完善功能

### 1. 实现完整的 RTSP 服务器

当前 RTSP 服务器只实现了基础信令，还需要：
- [ ] 实际的 RTP 数据包发送
- [ ] H.264/H.265 封包
- [ ] 多客户端支持
- [ ] SDP 动态生成

### 2. 实现 RTMP 服务器

- [ ] RTMP 协议解析
- [ ] 握手处理
- [ ] 流媒体解复用

### 3. 整合 HLS 输出

使用 FFmpeg 的 HLS muxer：
```cpp
#include <libavformat/avformat.h>

// 在 on_video_frame 中写入 HLS
av_interleaved_write_frame(hls_format_ctx, packet);
```

### 4. 集成 WebRTC

推荐使用 [libdatachannel](https://github.com/paullouisageneau/libdatachannel)：

```bash
# 安装
git clone https://github.com/paullouisageneau/libdatachannel.git
cd libdatachannel && cmake -B build && cmake --build build
sudo cmake --install build
```

然后在 `src/webrtc_sender_impl.cpp` 中实现真正的推送逻辑。

---

## 技术架构

```
┌─────────────────────────────────────────┐
│          你的媒体服务器                  │
│                                         │
│  ┌──────────┐  ┌──────────┐            │
│  │ RTSP     │  │ RTMP     │            │
│  │ Server   │  │ Server   │            │
│  │ (socket) │  │ (socket) │            │
│  └────┬─────┘  └────┬─────┘            │
│       │             │                   │
│       └──────┬──────┘                   │
│              │                          │
│       ┌──────▼──────┐                   │
│       │ 解复用器     │ (提取 H.264/AAC)   │
│       └──────┬──────┘                   │
│              │                          │
│       ┌──────┴──────┐                   │
│       │             │                   │
│  ┌────▼────┐  ┌────▼────┐              │
│  │ HLS     │  │ WebRTC  │              │
│  │ Muxer   │  │ Sender  │              │
│  │(FFmpeg) │  │(待实现)  │              │
│  └─────────┘  └─────────┘              │
└─────────────────────────────────────────┘
```

---

## 常见问题

### Q: 为什么推流后立即断开？

A: 当前 RTSP 服务器实现还不完整，只支持基础信令。完整实现需要处理 RTP 包发送。

### Q: HLS 文件在哪里？

A: 默认在 `./hls_output/` 目录下，需要等推流开始后才会生成。

### Q: 能同时接收多个推流吗？

A: 可以。每个流使用不同的路径：
- `rtsp://localhost:8554/live/cam1`
- `rtsp://localhost:8554/live/cam2`

### Q: 如何查看服务器状态？

A: 服务器会每 10 秒输出一次活跃推流列表：
```
[状态] 活跃推流：2
  - live/cam1 [视频] [音频]
  - live/cam2 [视频]
```

---

## 相关文件

- `include/media_server.hpp` - 媒体服务器接口
- `include/rtsp_server.hpp` - RTSP 服务器实现
- `src/media_server.cpp` - 服务器主逻辑
- `src/media_server_main.cpp` - 主程序入口
- `src/rtsp_server.cpp` - RTSP 协议实现

---

## 对比其他方案

| 方案 | 优点 | 缺点 |
|------|------|------|
| **你的实现** | 自主可控，代码清晰 | 需要继续完善 |
| **ZLMediaKit** | 功能完整，生产级 | 代码量大，学习成本高 |
| **SRS** | 功能强大，文档齐全 | Go/C++ 混合，较复杂 |
| **MediaMTX** | 轻量级，易部署 | 定制性差 |

---

## 许可证

MIT License

---

**祝你使用愉快！** 🎉

如有问题，查看日志输出或联系开发者。
