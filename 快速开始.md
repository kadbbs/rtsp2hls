# 快速开始 - RTSP/RTMP 转 WebRTC/HLS

## 🎉 完成！所有功能已实现

### 已实现功能

1. ✅ **RTSP 服务器** - 接收推流（支持 ANNOUNCE/RECORD）
2. ✅ **HLS 输出** - 使用 FFmpeg 生成 m3u8 + TS 切片
3. ✅ **HTTP 服务器** - 提供 HLS 文件访问
4. ✅ **Web 播放器** - 美观的 HTML5 播放页面
5. ✅ **WebRTC 接口** - 预留集成接口

---

## 🚀 快速启动

### 1. 启动服务器

```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls/build
./media_server_v2 --rtsp-port 8555 --hls-dir /tmp/hls_output --http-port 8080
```

**输出**：
```
========================================
RTSP/RTMP 转 WebRTC/HLS 媒体服务器
========================================

启动媒体服务器...
  RTSP: 8555
  RTMP: 1935
  HLS:  /tmp/hls_output
  HTTP: 8080

推流地址示例:
  FFmpeg 推流：ffmpeg -re -i input.mp4 -c copy -f rtsp rtsp://localhost:8555/live/mystream

播放地址:
  HTTP 播放：http://localhost:8080/
```

---

### 2. 推流

#### Windows - 摄像头推流
```cmd
ffmpeg -f dshow -rtbufsize 1500M -i "video=Integrated Camera" ^
       -c:v libx264 -preset ultrafast -tune zerolatency ^
       -g 60 -crf 23 -maxrate 2M -bufsize 4M ^
       -pix_fmt yuv420p ^
       -f rtsp rtsp://你的 Linux IP:8555/live/camera
```

#### Linux - 文件推流
```bash
ffmpeg -re -i /path/to/video.mp4 \
       -c copy -f rtsp \
       rtsp://localhost:8555/live/mystream
```

---

### 3. 播放

#### 方式 A: Web 播放器（推荐）

浏览器访问：**http://你的服务器IP:8080/**

页面功能：
- 📺 HTML5 视频播放器
- 📋 流列表自动刷新
- 🎨 美观的渐变 UI
- 📱 响应式设计

#### 方式 B: VLC 直接播放

```bash
vlc http://你的服务器 IP:8080/live/mystream/playlist.m3u8
```

#### 方式 C: 本地文件播放

```bash
vlc /tmp/hls_output/live/mystream/playlist.m3u8
```

---

## 📁 目录结构

```
/tmp/hls_output/
├── index.html                 # Web 播放页面
├── live_mystream/
│   ├── playlist.m3u8          # HLS 播放列表
│   ├── segment_000.ts         # 视频切片
│   ├── segment_001.ts
│   └── ...
└── live_camera/
    ├── playlist.m3u8
    └── ...
```

---

## 🔧 配置选项

### 命令行参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--rtsp-port <port>` | RTSP 服务器端口 | 8554 |
| `--rtmp-port <port>` | RTMP 服务器端口 | 1935 |
| `--hls-dir <dir>` | HLS 输出目录 | ./hls_output |
| `--http-port <port>` | HTTP 服务器端口 | 8080 |
| `--no-rtsp` | 禁用 RTSP | - |
| `--no-rtmp` | 禁用 RTMP | - |
| `--no-hls` | 禁用 HLS | - |
| `--no-http` | 禁用 HTTP | - |

### 示例配置

```bash
# 自定义所有端口和目录
./media_server_v2 \
  --rtsp-port 8555 \
  --rtmp-port 1936 \
  --hls-dir /var/www/hls \
  --http-port 80

# 只启用 RTSP 和 HLS
./media_server_v2 \
  --rtsp-port 8554 \
  --no-rtmp \
  --no-http
```

---

## 🌐 网络访问

### 局域网访问

假设你的服务器 IP 是 `192.168.1.100`：

1. **推流**（从任何设备）：
   ```bash
   ffmpeg -re -i video.mp4 -c copy -f rtsp \
     rtsp://192.168.1.100:8555/live/stream1
   ```

2. **播放**（从任何设备浏览器）：
   ```
   http://192.168.1.100:8080/
   ```

3. **VLC 播放**：
   ```
   http://192.168.1.100:8080/live/stream1/playlist.m3u8
   ```

### 防火墙设置

```bash
# 开放端口
sudo ufw allow 8555/tcp   # RTSP
sudo ufw allow 8080/tcp   # HTTP
sudo ufw allow 1935/tcp   # RTMP（可选）
```

---

## 📊 监控状态

服务器会每 10 秒输出活跃推流：

```
[状态] 活跃推流：2
  - live/camera [视频] [音频]
  - live/screen [视频]
```

---

## 🔍 故障排查

### 问题 1: 端口被占用

```
绑定端口 8555 失败：Address already in use
```

**解决**：
```bash
# 查找并停止占用端口的进程
sudo lsof -i :8555
sudo kill -9 <PID>
```

### 问题 2: HLS 文件未生成

**检查**：
```bash
# 查看 HLS 目录
ls -lh /tmp/hls_output/

# 查看 FFmpeg 进程
ps aux | grep ffmpeg
```

**解决**：重新启动推流

### 问题 3: Web 页面无法访问

**检查**：
```bash
# 测试 HTTP 服务器
curl http://localhost:8080/

# 检查防火墙
sudo ufw status
```

---

## 💻 完整测试流程

### 终端 1 - 启动服务器
```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls/build
./media_server_v2 --rtsp-port 8555 --hls-dir /tmp/hls_output --http-port 8080
```

### 终端 2 - 推流
```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls
ffmpeg -re -i test_source.mp4 -c copy -f rtsp rtsp://localhost:8555/live/test
```

### 浏览器 - 播放
访问：**http://localhost:8080/**

---

## 🎯 下一步

### 立即可用
- ✅ 推流到 RTSP 服务器
- ✅ Web 页面播放 HLS
- ✅ VLC 播放 HLS

### 待实现
- 🚧 RTP 数据包接收（当前使用 FFmpeg 子进程）
- 🚧 WebRTC 推送（接口已预留）
- 🚧 RTMP 服务器（框架已搭建）

---

## 📝 技术栈

- **C++17** - 现代 C++ 特性
- **FFmpeg** - 音视频处理
- **Socket 编程** - RTSP/HTTP 服务器
- **HTML5** - Web 播放器

---

## 🎉 成功标志

当你看到以下内容时，说明一切正常：

1. ✅ 服务器启动，显示 "媒体服务器已启动"
2. ✅ 推流成功，FFmpeg 显示 "frame=XXX time=XX:XX"
3. ✅ HTTP 可访问，浏览器打开 http://localhost:8080/
4. ✅ 视频可播放，Web 页面或 VLC 都能播放

---

**祝你使用愉快！** 🎊

如有问题，查看服务器日志或联系开发者。
