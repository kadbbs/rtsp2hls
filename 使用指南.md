# RTSP/RTMP 转 WebRTC/HLS - 完整使用指南

## 🎯 快速开始（5 分钟上手）

### 1. 编译项目

```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls
mkdir -p build && cd build
cmake .. && make -j4
```

编译成功后会生成两个可执行文件：
- `stream_converter` - 拉流模式（从现有 RTSP/RTMP 源拉流）
- `media_server_v2` - **服务器模式**（接收推流并转发）← 使用这个

---

### 2. 启动服务器

```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls/build
./media_server_v2 --rtsp-port 8554 --hls-dir /tmp/hls_output --http-port 8080
```

**输出示例**：
```
========================================
RTSP/RTMP 转 WebRTC/HLS 媒体服务器
========================================

启动媒体服务器...
  RTSP: 8554
  RTMP: 1935
  HLS:  /tmp/hls_output
  HTTP: 8080

推流地址示例:
  FFmpeg 推流：ffmpeg -re -i input.mp4 -c copy -f rtsp rtsp://localhost:8554/live/mystream

播放地址:
  HTTP 播放：http://localhost:8080/
```

---

### 3. 推流

#### 方式 A：从文件推流（测试用）

```bash
ffmpeg -re -i /path/to/video.mp4 -c copy \
  -f rtsp rtsp://localhost:8554/live/mystream
```

#### 方式 B：从摄像头推流（Windows）

```cmd
ffmpeg -f dshow -rtbufsize 1500M -i "video=Integrated Camera" ^
       -c:v libx264 -preset ultrafast -tune zerolatency ^
       -g 60 -crf 23 -maxrate 2M -bufsize 4M ^
       -pix_fmt yuv420p ^
       -f rtsp rtsp://你的 Linux IP:8554/live/camera
```

#### 方式 C：从摄像头推流（Linux）

```bash
ffmpeg -f v4l2 -i /dev/video0 -f alsa -i default \
  -c:v libx264 -preset ultrafast \
  -f rtsp rtsp://localhost:8554/live/camera
```

#### 方式 D：使用 OBS 推流

**OBS 设置**：
1. 设置 → 推流
2. 服务：自定义
3. 服务器：`rtmp://你的 IP:1935/live`
4. 推流码：`mystream`
5. 开始推流

---

### 4. 播放

#### Web 播放（推荐）

浏览器访问：**http://你的服务器 IP:8080/player.html**

1. 输入流路径：`live/mystream`
2. 点击 "▶️ 播放"

#### VLC 播放

```bash
vlc http://你的服务器 IP:8080/live/mystream/playlist.m3u8
```

#### 本地文件播放

```bash
vlc /tmp/hls_output/live_mystream/playlist.m3u8
```

---

## 📋 命令行参数

### media_server_v2 参数

| 参数 | 说明 | 默认值 | 示例 |
|------|------|--------|------|
| `--rtsp-port <port>` | RTSP 服务器端口 | 8554 | `--rtsp-port 8555` |
| `--rtmp-port <port>` | RTMP 服务器端口 | 1935 | `--rtmp-port 1936` |
| `--hls-dir <dir>` | HLS 输出目录 | ./hls_output | `--hls-dir /tmp/hls` |
| `--http-port <port>` | HTTP 服务器端口 | 8080 | `--http-port 8081` |
| `--no-rtsp` | 禁用 RTSP | - | `--no-rtsp` |
| `--no-rtmp` | 禁用 RTMP | - | `--no-rtmp` |
| `--no-hls` | 禁用 HLS | - | `--no-hls` |
| `--no-http` | 禁用 HTTP | - | `--no-http` |
| `-h, --help` | 显示帮助 | - | `-h` |

### 常用配置示例

#### 标准配置
```bash
./media_server_v2 \
  --rtsp-port 8554 \
  --hls-dir /tmp/hls_output \
  --http-port 8080
```

#### 自定义所有端口
```bash
./media_server_v2 \
  --rtsp-port 8555 \
  --rtmp-port 1936 \
  --hls-dir /var/www/hls \
  --http-port 80
```

#### 仅启用 RTSP 和 HLS
```bash
./media_server_v2 \
  --rtsp-port 8554 \
  --no-rtmp \
  --no-http
```

---

## 🔧 常见使用场景

### 场景 1：本地文件测试

```bash
# 终端 1 - 启动服务器
./media_server_v2 --rtsp-port 8554 --hls-dir /tmp/hls_output --http-port 8080

# 终端 2 - 推流
ffmpeg -re -i test_source.mp4 -c copy -f rtsp rtsp://localhost:8554/live/test

# 浏览器访问
# http://localhost:8080/player.html
# 输入：live/test
```

### 场景 2：摄像头实时监控

**Windows 推流**：
```cmd
ffmpeg -f dshow -rtbufsize 1500M -i "video=Integrated Camera" ^
       -c:v libx264 -preset ultrafast ^
       -f rtsp rtsp://192.168.1.100:8554/live/cam1
```

**Linux 服务器**：
```bash
./media_server_v2 --rtsp-port 8554 --hls-dir /var/www/hls --http-port 80
```

**手机/平板观看**：
```
http://192.168.1.100/live_cam1/playlist.m3u8
```

### 场景 3：多路摄像头监控

```bash
# 启动服务器
./media_server_v2 --rtsp-port 8554 --hls-dir /tmp/hls --http-port 8080

# 推流 1 号摄像头
ffmpeg -re -i cam1.mp4 -c copy -f rtsp rtsp://localhost:8554/live/cam1 &

# 推流 2 号摄像头
ffmpeg -re -i cam2.mp4 -c copy -f rtsp rtsp://localhost:8554/live/cam2 &

# 推流 3 号摄像头
ffmpeg -re -i cam3.mp4 -c copy -f rtsp rtsp://localhost:8554/live/cam3 &
```

**播放**：
- cam1: http://localhost:8080/player.html → 输入 `live/cam1`
- cam2: http://localhost:8080/player.html → 输入 `live/cam2`
- cam3: http://localhost:8080/player.html → 输入 `live/cam3`

### 场景 4：网络直播

```bash
# 服务器启动
./media_server_v2 \
  --rtsp-port 8554 \
  --hls-dir /var/www/html/live \
  --http-port 80

# OBS 推流
# 服务器：rtmp://你的服务器 IP:1935/live
# 推流码：stream1

# 观众访问
# http://你的服务器 IP/live_stream1/playlist.m3u8
```

---

## 📊 监控和维护

### 查看活跃推流

服务器会每 10 秒输出一次状态：
```
[状态] 活跃推流：2
  - live/cam1 [视频] [音频]
  - live/cam2 [视频]
```

### 查看 HLS 文件

```bash
# 查看输出目录
ls -lh /tmp/hls_output/

# 查看特定流
ls -lh /tmp/hls_output/live_mystream/

# 查看播放列表
cat /tmp/hls_output/live_mystream/playlist.m3u8
```

### 检查端口占用

```bash
# 查看端口监听
netstat -tlnp | grep 8554

# 或使用 ss
ss -tlnp | grep 8554
```

### 停止服务器

```bash
# Ctrl+C 停止前台运行的服务器

# 或杀死后台进程
pkill -f media_server_v2
```

---

## 🐛 故障排查

### 问题 1：端口被占用

**错误**：
```
绑定端口 8554 失败：Address already in use
```

**解决**：
```bash
# 查找占用端口的进程
sudo lsof -i :8554

# 或
sudo netstat -tlnp | grep 8554

# 杀死进程
sudo kill -9 <PID>

# 或更换端口
./media_server_v2 --rtsp-port 8555
```

### 问题 2：FFmpeg 推流失败

**错误**：
```
Connection refused
```

**解决**：
1. 检查服务器是否启动：`ps aux | grep media_server_v2`
2. 检查端口是否监听：`netstat -tlnp | grep 8554`
3. 检查防火墙：`sudo ufw allow 8554/tcp`
4. 使用正确的 IP 地址（不是 localhost）

### 问题 3：HLS 文件未生成

**检查**：
```bash
# 查看目录
ls -lh /tmp/hls_output/

# 查看服务器日志
# 应该看到 "[RTP] 收到视频帧" 的日志
```

**解决**：
1. 确保推流成功（FFmpeg 显示 frame=XXX time=XX:XX）
2. 检查流路径是否正确
3. 等待 5-10 秒让 HLS 生成

### 问题 4：Web 播放器无法播放

**检查**：
```bash
# 测试 HTTP 服务器
curl http://localhost:8080/

# 测试 HLS 文件
ffprobe /tmp/hls_output/live_mystream/playlist.m3u8
```

**解决**：
1. 确保 HLS 文件已生成
2. 检查浏览器控制台错误（F12）
3. 尝试用 VLC 直接播放 HLS 文件

---

## 📈 性能优化

### 降低延迟

```bash
# 使用更短的切片时长
./media_server_v2 \
  --hls-dir /tmp/hls_output \
  # HLS 切片 1 秒（默认 2 秒）
```

修改代码中的 `segment_duration` 参数为 1。

### 减少 CPU 使用

```bash
# FFmpeg 推流时使用更快的预设
ffmpeg -i input.mp4 -c:v libx264 -preset ultrafast ...
```

### 提高稳定性

```bash
# 使用 systemd 管理进程
sudo nano /etc/systemd/system/media-server.service
```

内容：
```ini
[Unit]
Description=RTSP/RTMP Media Server
After=network.target

[Service]
ExecStart=/home/bs/code/rtsp_rtmp2webrtc_hls/build/media_server_v2 \
  --rtsp-port 8554 \
  --hls-dir /tmp/hls_output \
  --http-port 8080
Restart=always
User=bs

[Install]
WantedBy=multi-user.target
```

启用：
```bash
sudo systemctl daemon-reload
sudo systemctl enable media-server
sudo systemctl start media-server
```

---

## 🔐 安全配置

### 防火墙设置

```bash
# Ubuntu/Debian
sudo ufw allow 8554/tcp   # RTSP
sudo ufw allow 1935/tcp   # RTMP
sudo ufw allow 8080/tcp   # HTTP
sudo ufw enable

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=8554/tcp
sudo firewall-cmd --permanent --add-port=1935/tcp
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
```

### 限制访问（未来功能）

目前所有功能都是公开的，建议：
1. 使用防火墙限制 IP
2. 放在内网使用
3. 通过 VPN 访问

---

## 📚 进阶使用

### 自定义流路径

```bash
# 推流到自定义路径
ffmpeg -re -i video.mp4 -c copy -f rtsp \
  rtsp://localhost:8554/myapp/mystream

# 播放
# http://localhost:8080/player.html
# 输入：myapp/mystream
```

### 多路复用

```bash
# 同时推流音频和视频
ffmpeg -re -i video.mp4 -i audio.mp3 \
  -c:v copy -c:a aac \
  -f rtsp rtsp://localhost:8554/live/av
```

### 录制推流

```bash
# 同时推流和录制
ffmpeg -re -i input.mp4 \
  -c copy -f rtsp rtsp://localhost:8554/live/stream \
  -c copy -f mp4 recording.mp4
```

---

## 🎯 完整示例

### 家庭监控系统

**硬件**：
- Linux 服务器（你的电脑）
- IP 摄像头或多个 USB 摄像头

**步骤**：

1. **启动服务器**
```bash
cd /home/bs/code/rtsp_rtmp2webrtc_hls/build
./media_server_v2 --rtsp-port 8554 --hls-dir /var/www/html/monitor --http-port 80
```

2. **推流（每个摄像头）**
```bash
# 摄像头 1
ffmpeg -f v4l2 -i /dev/video0 -f alsa -i default \
  -c:v libx264 -preset ultrafast \
  -f rtsp rtsp://localhost:8554/live/cam1 &

# 摄像头 2
ffmpeg -f v4l2 -i /dev/video1 -f alsa -i default \
  -c:v libx264 -preset ultrafast \
  -f rtsp rtsp://localhost:8554/live/cam2 &
```

3. **手机观看**
```
http://你的服务器 IP/monitor/live_cam1/playlist.m3u8
http://你的服务器 IP/monitor/live_cam2/playlist.m3u8
```

4. **创建监控页面**
```html
<!DOCTYPE html>
<html>
<head><title>家庭监控</title></head>
<body>
  <h1>摄像头 1</h1>
  <video src="/monitor/live_cam1/playlist.m3u8" controls autoplay></video>
  
  <h1>摄像头 2</h1>
  <video src="/monitor/live_cam2/playlist.m3u8" controls autoplay></video>
</body>
</html>
```

---

## 📞 获取帮助

### 查看日志

服务器日志包含所有重要信息：
```bash
# 前台运行直接看到日志
./media_server_v2 ...

# 或查看系统日志
journalctl -u media-server -f
```

### 调试模式

```bash
# 启用详细日志（修改代码后重新编译）
# 在相关函数中添加 log() 调用
```

### 测试工具

```bash
# 测试 RTSP 流
ffprobe rtsp://localhost:8554/live/test

# 测试 HLS
ffprobe /tmp/hls_output/live_test/playlist.m3u8

# 测试 HTTP
curl http://localhost:8080/
```

---

## 🎊 总结

你现在拥有了一个**完整的流媒体服务器**！

**功能**：
- ✅ RTSP 推流接收
- ✅ RTMP 推流接收
- ✅ HLS 输出生成
- ✅ HTTP Web 服务
- ✅ Web 播放器

**使用流程**：
1. 启动服务器
2. 推流（FFmpeg/OBS/摄像头）
3. Web 或 VLC 播放

**祝你使用愉快！** 🎉
